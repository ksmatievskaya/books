import './spinner.scss'

const Spinner = () => {
    return (
        <div className="spinner">
            <span className="spinner_text">Loading...</span>
        </div>
    )
}

export default Spinner;